<UserControl x:Class="CastSeen.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CastSeen.Views"
             xmlns:vm="clr-namespace:CastSeen.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:HomeViewModel/>
    </UserControl.DataContext>
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Welcome to CastSeen!" Foreground="{StaticResource col_foreground}" FontSize="40" FontFamily="{StaticResource font_headers}" Margin="0,0,0,20"/>
            <TextBlock Text="Explore the database to find when you've seen that actor before." Foreground="{StaticResource col_muted_foreground}" FontSize="18" FontFamily="{StaticResource font_text}" Margin="0,0,0,20" TextWrapping="Wrap"/>

            <Border Background="{StaticResource col_card}" Padding="20" CornerRadius="8" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Movies: " Foreground="{StaticResource col_foreground}" FontSize="20" FontFamily="{StaticResource font_text}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalMovies}" Foreground="{StaticResource col_primary}" FontSize="20" FontFamily="{StaticResource font_text}"/>
                </StackPanel>
            </Border>
            <Border Background="{StaticResource col_card}" Padding="20" CornerRadius="8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Total Actors: " Foreground="{StaticResource col_foreground}" FontSize="20" FontFamily="{StaticResource font_text}" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalActors}" Foreground="{StaticResource col_primary}" FontSize="20" FontFamily="{StaticResource font_text}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
