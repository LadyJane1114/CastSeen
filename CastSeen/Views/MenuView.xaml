<UserControl x:Class="CastSeen.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CastSeen.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="{StaticResource col_card}" Width="160">
        <StackPanel Margin="16">
            <TextBlock FontSize="24" Foreground="{StaticResource col_foreground}" FontFamily="{StaticResource font_headers}">
                Cast<Run Foreground="{StaticResource col_primary}" FontStyle="Italic" FontSize="26">Seen</Run>
            </TextBlock>
            <TextBlock Text="For when you've seen that actor before..." FontSize="10" FontFamily="{StaticResource font_text}" Foreground="{StaticResource col_accent}" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Height="1" Background="{StaticResource col_muted}" Margin="16,2"/>

        <ListBox ItemsSource="{Binding NavItems}" 
                 SelectedItem="{Binding SelectedNavItem}"
                 Background="{StaticResource col_card}" 
                 BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{Binding Icon}" 
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" 
                              Width="16" 
                              Height="16" 
                              Stretch="Uniform"
                              Margin="8"/>
                        <TextBlock Text="{Binding Label}" Margin="8" Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" Padding="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Height="1" Background="{StaticResource col_muted}" Margin="16,2"/>

        <Button Command="{Binding ExitCommand}" Style="{StaticResource SidebarButtonStyle}" Width="96" Margin="8" Padding="6">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Path Data="{StaticResource icon_exit}"
                      Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                      Width="16" Height="16" Stretch="Uniform" Margin="0,0,8,0" />
                <TextBlock Text="Exit" Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" VerticalAlignment="Center"/>
            </StackPanel>
        </Button>
    </StackPanel>
</UserControl>
