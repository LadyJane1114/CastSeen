<UserControl x:Class="CastSeen.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CastSeen.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="{StaticResource col_card}" Width="160">
        <StackPanel Margin="16">
            <TextBlock FontSize="24" Foreground="{StaticResource col_foreground}" FontFamily="{StaticResource font_headers}">
                Cast<Run Foreground="{StaticResource col_primary}" FontStyle="Italic" FontSize="26">Seen</Run>
            </TextBlock>
            <TextBlock Text="For when you've seen that actor before..." FontSize="11" FontFamily="{StaticResource font_text}" Foreground="{StaticResource col_secondary}" TextWrapping="Wrap"/>
        </StackPanel>

        <Border Height="1" Background="{StaticResource col_muted}" Margin="16,2"/>

        <ListBox ItemsSource="{Binding NavItems}" Background="{StaticResource col_card}" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding Command}" Background="Transparent" BorderThickness="0" Padding="8">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Icon}" Width="16" Height="16"/>
                            <TextBlock Text="{Binding Label}" Margin="8,0,0,0" Foreground="{StaticResource col_foreground}"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </StackPanel>
</UserControl>
